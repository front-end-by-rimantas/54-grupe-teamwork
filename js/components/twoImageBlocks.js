export function twoImageBlocks() {
    const HTML = `
        <section class="container">
            <div class="row">
                <div class="col-12">
                    SUBMIT LISTING + USER DASHBOARD CONTENT
                </div>
            </div>
        </section>`;
    document.body.insertAdjacentHTML('beforeend', HTML);
}
