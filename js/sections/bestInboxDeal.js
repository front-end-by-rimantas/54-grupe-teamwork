export function bestInboxDeal() {
    const HTML = `
        <section class="container bg-newsletter">
            <div class="row">
                <div class="col-12">
                    BEST INBOX DEAL CONTENT
                </div>
            </div>
        </section>`;
    document.body.insertAdjacentHTML('beforeend', HTML);
}
